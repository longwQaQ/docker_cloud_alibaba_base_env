# 贡献指南

## 提交问题

请说明操作系统、CPU 架构、Docker/Compose 版本、执行命令、脱敏日志、预期结果和实际结果。不要提交 `.env`、访问令牌、数据库内容或其他凭证。

安全问题请遵循 [SECURITY.md](SECURITY.md)，不要公开披露利用细节。

## 提交变更

1. 先通过 Issue 说明问题和预期行为；
2. 保持变更范围单一，不混入无关格式化；
3. 不得引入远程脚本直接执行、特权容器、Docker Socket 挂载、宿主机根目录挂载或默认公网端口；
4. 新增下载内容必须使用可信上游地址，并提供完整性校验；
5. 修改组件版本时必须同步 Schema、配置、文档和测试；
6. 运行 `./scripts/validate.sh`，并在 Pull Request 中说明实际启动验证结果。

所有变更都需人工审核后合并。除非贡献者明确另行声明，主动提交并被项目接纳的贡献将依照项目的 [Apache License 2.0](LICENSE) 授权。
