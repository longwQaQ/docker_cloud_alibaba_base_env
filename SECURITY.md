# 安全策略

## 支持范围

当前仅维护 `main` 分支。本项目用于本地开发和集成测试，不提供生产环境安全承诺。

## 威胁模型

项目会运行第三方容器镜像和官方 Release JAR，读取本地 `.env` 凭证，开放本机端口，并让 Nacos 与 Seata 访问 MySQL。重点信任边界包括：

- Docker daemon 与宿主机；
- 本机端口与其他本地进程；
- Compose 配置、镜像/JAR 来源和上游发布渠道；
- MySQL、Nacos、Sentinel、Seata 之间的认证与网络访问；
- Pull Request 对镜像、构建脚本、挂载、端口和初始化 SQL 的修改。

## 已知限制

- Nacos 管理员账号由一次性初始化容器调用官方接口创建；删除数据卷后会重新初始化。
- 镜像使用明确版本标签，但尚未固定多架构 OCI digest；上游标签漂移仍属于供应链风险。
- 内部组件间数据库连接未启用 TLS，因为默认部署限定在 Docker 内部网络。
- 本项目不替代镜像漏洞扫描、主机加固、网络防火墙或生产级密钥管理系统。

## 报告漏洞

请不要在公开 Issue 中披露未修复漏洞、凭证或可直接利用的攻击细节。请通过 GitHub 的 **Private vulnerability reporting** 功能提交报告；如果该功能不可用，请先创建一个不含敏感细节的 Issue，请求维护者提供私密联系方式。

报告应包含受影响文件或版本、复现条件、潜在影响和建议修复方向。维护者会确认收到报告，并在完成验证后协调修复和披露。
